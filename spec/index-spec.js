require('../src');

describe('isSparse package', function() {

  xit('should not add isSparse if the Array object already has an isSparse method defined', function() {

  });

  xit('should add isSparse if Array.isSparse is not defined', function() {

  });
});
